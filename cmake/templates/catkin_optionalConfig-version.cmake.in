# Check whether the requested PACKAGE_FIND_VERSION is compatible
if("@PROJECT_VERSION@" VERSION_LESS "${PACKAGE_FIND_VERSION}")
  set(PACKAGE_VERSION_COMPATIBLE FALSE)
else()
  set(PACKAGE_VERSION_COMPATIBLE TRUE)
  if ("@PROJECT_VERSION@" VERSION_EQUAL "${PACKAGE_FIND_VERSION}")
    set(PACKAGE_VERSION_EXACT TRUE)
  endif()
endif()
message(STATUS "Found @PROJECT_NAME@: version @PROJECT_VERSION@ (required is >= \"${PACKAGE_FIND_VERSION}\") in ${CMAKE_CURRENT_LIST_DIR}")
